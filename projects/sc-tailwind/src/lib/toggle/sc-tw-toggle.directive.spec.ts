import { ScTwToggleDirective } from './sc-tw-toggle.directive';

describe('ScTwToggleDirective', () => {
  it('should create an instance', () => {
    const directive = new ScTwToggleDirective();
    expect(directive).toBeTruthy();
  });
});
