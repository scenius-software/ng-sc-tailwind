import { ToggleHandleDirective } from './toggle-handle.directive';

describe('ToggleHandleDirective', () => {
  it('should create an instance', () => {
    const directive = new ToggleHandleDirective();
    expect(directive).toBeTruthy();
  });
});
