import { ScTwNavItemDirective } from './sc-tw-nav-item.directive';

describe('ScTwNavItemDirective', () => {
  it('should create an instance', () => {
    const directive = new ScTwNavItemDirective();
    expect(directive).toBeTruthy();
  });
});
