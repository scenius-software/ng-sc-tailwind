import { Component, Input, Output, EventEmitter } from '@angular/core';

@Component({
  selector: 'storybook-modal',
  template: ` <div class="flex">Modal test</div> `,
})
export default class ModalComponent {}
